# build output
dist/

# large PokéAPI artwork — re-downloaded by `npm run sprites` (see scripts/sprites.mjs)
src/sprites/art/

# raw scrape cache: the parsed JSON in _research/ is committed, the source HTML is not
_research/serebii/
_research/*.html
_research/tsv/
_research/received.json

node_modules/
.DS_Store
Thumbs.db

# item icons and habitat pictures — re-downloaded by `npm run sprites`
src/sprites/items/
src/sprites/habitats/
# whole Bulbapedia species pages, fetched only to fill the Basin gaps in likes.json —
# the parsed _research/likes.json is committed, these 2 MB of source are not
_research/bulba_spin_*.json

# outfit thumbnails — re-downloaded by `npm run sprites`
src/sprites/outfits/
